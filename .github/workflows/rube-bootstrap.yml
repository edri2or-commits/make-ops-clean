name: Rube Bootstrap

on:
  workflow_dispatch:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "✅ Rube Bootstrap Workflow is active!"
