name: Enable Rube File Control

on:
  workflow_dispatch:
  workflow_dispatch:

jobs:
  file-control:
    runs-on: ubuntu-latest
    steps:
      - name: Echo control enabled
        run: echo "✅ File control enabled for Rube"
